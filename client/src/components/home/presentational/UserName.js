import styled from 'styled-components';

const UserName = styled.div`
  width: auto;
  margin: auto 0;
  margin-left: 16px;
  font-weight: 600;
  font-size: 14px;
  color: #262626;
`;

export default UserName;
