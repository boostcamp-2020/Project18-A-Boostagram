import React from 'react';
import styled from 'styled-components';

const style = {};
style.InputTagContainer = styled.div`
  background-color: gray;
`;

const InputTag = () => (
  <style.InputTagContainer>태그 검색 로직 필요</style.InputTagContainer>
);

export default InputTag;
